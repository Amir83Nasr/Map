do not use RASTER map always use VECTOR mapl

keep update files in DOCS folder.

keep update DEVELOPER SECTION DOCUMENT for developers.

After every change that affects the package API, usage, or structure, update the developer section in the same turn: in-map docs (`controls.developers` → `.qp-docs`), `docs/ARCHITECTURE.md`, and the developers part of `README.md`. Keep package usage simple and user-friendly (3-line React setup: two CSS imports + one component).
